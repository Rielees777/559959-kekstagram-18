'use strict';
(function () {
  var keyCode = {
    'ESC': 27,
    'ENTER': 13
  };
  var timeoutInterval = {
    DEBOUNCE_INTERVAL: 500
  };

  window.constants = {
    keyCode: keyCode,
    timeoutInterval: timeoutInterval
  };
})();
